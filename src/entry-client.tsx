// @refresh reload
import { mount, StartClient } from '@solidjs/start/client'

mount(() => <StartClient />, document.getElementById('app')!) // eslint-disable-line @typescript-eslint/no-non-null-assertion
